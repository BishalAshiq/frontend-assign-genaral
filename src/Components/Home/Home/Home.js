import React from 'react';
import Navigation from '../Nav/Navigation';

const Home = () => {
    return (
        <div>
            <Navigation></Navigation>
        </div>
    );
};

export default Home;